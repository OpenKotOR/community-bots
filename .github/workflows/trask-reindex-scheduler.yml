name: Trask reindex scheduler Worker

on:
  push:
    branches:
      - main
    paths:
      - "infra/trask-reindex-scheduler/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/trask-reindex-scheduler.yml"
  pull_request:
    paths:
      - "infra/trask-reindex-scheduler/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/trask-reindex-scheduler.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: trask-reindex-scheduler-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify-bundle:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.11.0

      - name: Install scheduler worker package
        run: pnpm install --frozen-lockfile --filter @openkotor/trask-reindex-scheduler

      - name: Typecheck
        run: pnpm --dir infra/trask-reindex-scheduler run check

      - name: Wrangler deploy dry-run
        run: pnpm dlx wrangler@4.87.0 deploy --config infra/trask-reindex-scheduler/wrangler.toml --dry-run

  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: verify-bundle
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.11.0

      - name: Install scheduler worker package
        run: pnpm install --frozen-lockfile --filter @openkotor/trask-reindex-scheduler

      - name: Deploy Worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          if [ -z "$CLOUDFLARE_API_TOKEN" ] || [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
            echo "::notice::Skipping deploy — set CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID."
            exit 0
          fi
          pnpm dlx wrangler@4.87.0 deploy --config infra/trask-reindex-scheduler/wrangler.toml

      - name: Sync indexer reindex URL (optional)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          TRASK_INDEXER_REINDEX_URL: ${{ vars.TRASK_INDEXER_REINDEX_URL }}
        run: |
          if [ -z "$CLOUDFLARE_API_TOKEN" ] || [ -z "$CLOUDFLARE_ACCOUNT_ID" ] || [ -z "$TRASK_INDEXER_REINDEX_URL" ]; then
            echo "::notice::Skipping TRASK_INDEXER_REINDEX_URL secret sync — set repo variable + CF creds."
            exit 0
          fi
          echo -n "$TRASK_INDEXER_REINDEX_URL" | pnpm dlx wrangler@4.87.0 secret put TRASK_INDEXER_REINDEX_URL --config infra/trask-reindex-scheduler/wrangler.toml
